/* ============================================================
   StaFlow — Configuração do Google Analytics 4 (FRONTEND)
   ------------------------------------------------------------
   ★ 18/08/2026 — a pedido do Breno, pra medir visitas, cliques e
   cadastros no site (complementa a atribuição gravada no banco —
   ver js/attribution.js e sql/029_atribuicao_marketing.sql).

   Propriedade "StaFlow" criada pelo Breno em 18/08/2026 no Google
   Analytics (analytics.google.com), fluxo de dados Web apontando
   pra https://staflow.app.br. Se precisar trocar de propriedade um
   dia, o ID de mensuração fica em Admin → Fluxos de dados → Web.

   Se este valor voltar a ser null por qualquer motivo, js/analytics.js
   simplesmente não carrega o Google Analytics — o site continua
   funcionando normalmente, só não fica sendo medido.
   ============================================================ */

window.STAFLOW_GA4_MEASUREMENT_ID = "G-NBM7ZLPB9Z";
